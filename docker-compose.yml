services:
  mailpit:
    image: axllent/mailpit:latest
    ports:
      - '${MAILPIT_HTTP_PORT:-8026}:8025'
      - '${MAILPIT_SMTP_PORT:-1026}:1025'
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 'true'
      MP_SMTP_AUTH_ALLOW_INSECURE: 'true'
